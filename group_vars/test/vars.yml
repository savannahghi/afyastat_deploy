---

# MySQL root password
mysql_root_pwd: "{{ mysql_root_pwd }}"

# Openmrs db settings
db_host: "{{ db_host }}"
db_name: "{{ db_name }}"
db_user: "{{ db_user }}"
db_password: "{{ db_password }}"
db_port: "{{ db_port }}"

# AfyaStat host settings
server_user: "{{ server_user }}"
server_domain: "{{ server_domain }}"
server_user_password: "{{ server_user_password }}"
